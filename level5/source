void n(void)
{
    char local_20c [520];

    // Read user input (up to 512 bytes)
    fgets(local_20c, 0x200, stdin);

    // VULNERABLE: Call printf with user input as format string
    printf(local_20c);

    // Exit after printing
    exit(1);
    return;
}

void main(void)
{
    n();
    return;
}
