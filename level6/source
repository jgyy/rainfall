#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void n(void)
{
    system("/bin/cat /home/user/level7/.pass");
}

void m(void)
{
    puts("Nope");
}

int main(int argc, char **argv)
{
    char    *buf;
    void    (**fp)(void);

    buf = malloc(64);
    fp  = malloc(4);

    *fp = m;
    strcpy(buf, argv[1]);
    (*fp)();

    return 0;
}
